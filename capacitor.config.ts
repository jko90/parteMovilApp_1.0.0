import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ec.mil.jercito.siper',
  appName: 'Parte Móvil',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
